# {{ managed_message }}
---
name: Flake8

on:
  push:
    branches-ignore:
      - 'repo-helper-update'
      - 'pre-commit-ci-update-config'
      - 'imgbot'
  pull_request:

jobs:
  Run:
    name: "Flake8"
    permissions:
      contents: read
    runs-on: "ubuntu-22.04"

    steps:
      - name: Checkout 🛎️
        uses: "actions/checkout@v4"{% if checkout_submodules %}
        with:
          submodules: {{ checkout_submodules }}{% endif %}

      - name: Check for changed files
        uses: dorny/paths-filter@v2
        id: changes
        with:
          list-files: "json"
          filters: |
            code:
              - '{{ code_file_filter }}'

      - name: Setup Python 🐍
        if: steps.changes.outputs.code == 'true'
        uses: "actions/setup-python@v5"
        with:
          python-version: "{{ python_deploy_version }}"

      - name: Install dependencies 🔧
        if: steps.changes.outputs.code == 'true'
        run: |
          python -VV
          python -m site
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install tox~=3.0

      - name: "Run Flake8"
        if: steps.changes.outputs.code == 'true'
        run: "python -m tox -e lint -s false -- --format github"
